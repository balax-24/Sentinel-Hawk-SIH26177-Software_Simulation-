# SIH Full System Integration Package (`sih_system`)

Master integration package connecting all autonomous subsystems into a unified mission execution.

## Full Mission Launch Command
```bash
ros2 launch sih_system full_mission.launch.py
```

## Running Headless (Docker / Remote Server / Cloud)
```bash
ros2 launch sih_system full_mission.launch.py gui:=false
```
